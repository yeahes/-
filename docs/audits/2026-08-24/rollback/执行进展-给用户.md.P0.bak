# 执行进展

更新时间：2026-08-24

## C1：审校清单漏标实测

这一步只读，没有改字幕、音频、缓存，也没有往旧结果里写 `editor-review-ledger.json`。

我拿“最年轻人不爱留学”那次人工终稿，统计用户真正动过的父字幕，再拿同一次自动运行的原始结果，用程序现有的 `load_subtitle_review_marks()` 回填标记，直接做交集。

结果很直白：

```text
用户改过：18 条父字幕
程序标中：5 条
程序漏掉：13 条
```

命中率是 27.78%。漏掉的 ID 和英文原文已经逐条写进审计报告 §21.7。

命中的类别主要是：

- `english_cut`：S0034；
- `visual_page`：S0121、S0230；
- `chinese_length`：S0213、S0217、S0230。

六份人工终稿文件里，真正有用户修改记录的只有这一集 stable run；两个 AI checkpoint 和一个年轻人 checkpoint 的 edits 都是空记录。年轻人 stable run 下面还发现了三层“人工终稿字幕包”套娃目录，三个文件内容和大小不同，确认存在打包递归问题；本轮只记录，没有修。

## 这说明什么

之前关于“审校清单完全不工作”的结论是错的，实际清单确实在标记；但这次干净样本说明它可能漏掉不少用户最终会改的地方。现在还不能直接改规则，因为漏标原因还没有逐条归类。

## A：切分边界安全复核

A 已完成，数字全部对上：

```text
生产已接受边界：5180 条
当前规则判非法：452 条
open_subordinate_prefix_fragment：37 条
候选门内切点：3537 条
arm F 新增合法：69 条
新增非法：0 条
```

这里发现审计目录里的 `measure_open_subordinate_v4.py` 还是旧接线，只替换共享 helper；现在 arm F 已经是私有判据，所以它直接跑出 F=0 是测量脚本过时。用旧私有判据和当前私有判据做对照后，正确结果是 `69 / 0`。这没有改生产代码。

## 下一步

B、D、E 的测量都完成了。C1 的 13 条漏标暂时只作为事实记录，不拿来放宽任何规则；补信号要等逐条归因后再决定。

## E：分页宽度三档对照

只做了离线对照图，没有改常数，也没有选择方案。当前直接产物里的 3343 个双行页统计为：

```text
1100：19 页可改单行
1260：310 页可改单行
1455：792 页可改单行
```

所以 1260 比 1100 多 291 页，1455 比 1260 再多 482 页。图和测量数据在：
`docs/audits/2026-08-24/pagination-threshold-comparison/`。

报告早先的冻结快照写的是 303 / 785；当前工作树直接产物复测是 310 / 792，差异已记录，没有偷偷改目标值。

## 需要决定的事

## 历史人工参考集（不作为 F 新素材）

用户确认以下四个结果都经过人工调整并已合成视频：

- `无论怎么衡量，就业市场都很疲软`
- `中国人会爱上巧克力吗？`
- `烂到爆红：一部动画的逆袭`
- `中式梦核：千禧一代的怀旧密码`

它们可以用于回溯比较自动结果与用户最终选择，并作为防退化参考；由于用户也指出人工调整可能存在不完美，不能直接把全部人工修改当作绝对真值。它们都不是 F 所要求的“未人工校对新音频”，因此不能替代 F。

E 只输出 1100 / 1260 / 1455 三档的真实渲染对照和受影响页数，不替你决定哪一档。
工作树里 `ARTICLE_SUBTITLE_EN_PREFERRED_LINE_WIDTH` 当前已经是 1260，这是本轮之前
已有的改动；本轮没有再改它。E 完成后是否保留 1260，需要你根据对照图决定。

F 还缺一个合规的新音频：桌面上目前没有明确的新音频，现有白宫、巧克力、就业、
日本 X 世代等都不能按硬规则重跑。没有新素材前不执行 F，也不把旧案例结果算作
F 的通过证据。
