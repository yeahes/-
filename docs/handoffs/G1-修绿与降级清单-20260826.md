# G1' 修绿与降级清单报告

时间：2026-08-26 12:39:17 Asia/Shanghai

## 前提核验

| 前提 | 结果 | 证据 |
|---|---|---|
| A1 HEAD=fdd9d83、分支 main | 成立 | `git rev-parse --short HEAD`=`fdd9d83`；`git branch --show-current`=`main` |
| A2 基线副本仍在 | 成立 | `git worktree list` 包含 `E:/vc-head-baseline fdd9d83 (detached HEAD)` |
| A3 S0038 计划失败关闭且不泄漏 bundle | 成立 | `test_no_safe_normal_font_partition_fails_closed_instead_of_using_50px` 断言 `plan` 与 `bundle` 均为 `render_structural_overflow`，均通过 |
| A4 screen_editor.py 位置 | 成立 | `app/core/subtitle_processor/screen_editor.py` 存在 |

## 单测与回归

| 验证 | 结果 |
|---|---|
| `test_no_safe_normal_font_partition_fails_closed_instead_of_using_50px` | PASS |
| `test_high_pressure_secondary_review_rejects_incomplete_phrase_boundaries` | PASS；S9513 不再从谓语/宾语中间切开 |
| `test_renderable_review_fallback_is_degraded_without_blocking_the_blueprint` | PASS；新增数量字段断言也通过 |
| `python -m py_compile`（两个生产文件及目标测试） | PASS |
| `--only article-display-readability-contract` 收尾回归 | FAIL（既有 S9522 断言，HEAD 基线副本同样 FAIL；未改选择逻辑） |

收尾回归唯一失败：`tests/test_article_display_readability_contract.py:3251`，S9522 实际切分为 `... expansion engine | in the modern ...`，测试期望为 `... into the most aggressive expansion engine ... | in the modern ...`。日志：`output/regression-logs/article-display-readability-contract-20260826T121921.307591.log`。

## 女性集离线证据

输入为冻结 checkpoint `work-dir/中国职场女性为何悄然掉队？/subtitle/stable-checkpoints/20260826T040659.244182-79951e43`。已有只读回放记录（`docs/handoffs/M1-女性集离线取数-20260826.md` 与 `output/m1-blueprint-diff-20260826.json`）显示：

| 指标 | 数字 |
|---|---:|
| 蓝图 status | `PASS` |
| errors | `0` |
| degraded 父句数 | `1` |
| degraded 父句 | `S0089` |
| 总父句数 | `271` |
| degraded 父句比例 | `1 / 271 = 0.369%` |
| 2% 数量闸门 | `5`（`floor(271 × 0.02)`，至少保留 1 个小样本出口） |
| 总页数 | `306` |
| 内容变化页数 | `0` |
| G1 归因页数 | `0` |
| G7 归因页数 | `0` |
| `page_artifact["status"] == "PASS"` | 成立 |

本轮再次启动蓝图重算超过一分钟无输出，按 Verification Tiering 停止；没有并行重跑，也没有写入 checkpoint。上表沿用已完成的只读回放证据。

### 降级上屏校对清单（只读整理）

生产写入路径：`<stable-artifact-dir>/degraded-review-checklist.jsonl`，每个 degraded 父句一行。冻结 checkpoint 未写入该文件；以下为从冻结分页计划和页翻译记录整理出的唯一一行：

```json
{
  "cue_index": 89,
  "parent_subtitle_id": "S0089",
  "pages": [
    {
      "display_page_id": "S0089.P01",
      "english": "Now, these banquets have been increasingly restricted recently",
      "chinese": "近来，这些饭局日益受到限制，"
    },
    {
      "display_page_id": "S0089.P02",
      "english": "by new compliance and disciplinary guidelines,",
      "chinese": "来自新的合规与纪律准则。"
    }
  ],
  "degraded_reasons": ["no_complete_normal_font_page_partition"]
}
```

S0089 两页均 `renderable=true`、`degraded=true`，最终英文均为 `56px`；该父句在 2% 闸门内。

## 本轮改动

- `podcast_learning_video.py`：删除固定 8 个上限，数量闸门按命名常量 `ARTICLE_DISPLAY_DEGRADED_MAX_RATIO=0.02` 计算；PASS/ERROR 蓝图都输出 `total_parent_count`、`degraded_parent_ratio`、`degraded_page_threshold`。
- `screen_editor.py`：G1 的降级清单写入逻辑已存在，路径为每集稳定 artifacts 目录下的 `degraded-review-checklist.jsonl`。
- `tests/test_article_display_readability_contract.py`：补充蓝图数量字段断言；未删除或改写既有断言。

## 发现但没动

- `tests/test_article_display_readability_contract.py:3251` 的 S9522 页面选择断言；基线同样失败，属于相邻选择逻辑。
- 测试期直接单函数调用仍可能触发 `AppData/logs/app.log` 的 `WinError 32` 轮转噪音；未修改生产日志配置。
- 工作树已有大量跨轮改动、CRLF 噪音和生成文件；未清理、未回退。

## 提交与安全边界

本轮未执行 `git add`、`git commit`、`git push`、`git stash`、`git restore` 或 `git checkout`。未修改 `D:\软件缓存\VideoCaptioner`，未写入或移动冻结 checkpoint 产物；基线副本 `E:/vc-head-baseline` 保留。
