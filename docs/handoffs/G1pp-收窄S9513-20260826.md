# G1'' 前提冲突与差异报告

时间：2026-08-26 12:59:05 Asia/Shanghai

## 前提核验

| 前提 | 结果 | 证据 |
|---|---|---|
| A1 HEAD=fdd9d83、分支 main | 成立 | 当前树 `git rev-parse --short HEAD`=`fdd9d83`；`git branch --show-current`=`main` |
| A2 基线副本仍在 | 成立 | `git worktree list` 包含 `E:/vc-head-baseline fdd9d83 (detached HEAD)` |
| A3 基线 S9522 测试 PASS | **不成立** | 从基线目录导入 `E:/vc-head-baseline/tests/test_article_display_readability_contract.py` 后，测试在基线第 3504 行 FAIL |

## A3 失败证据

在基线副本中使用同一个 runtime：

```text
E:\VideoCaptioner-screen-subtitle\runtime\python.exe
```

并显式把 `E:\vc-head-baseline` 放到 `sys.path` 首位，确认没有导入当前工作树的测试文件。基线函数调用结果：

```text
AssertionError: E:\vc-head-baseline\tests\test_article_display_readability_contract.py:3504
```

基线实际分页：

```text
[
  "You're getting plugged directly into the most aggressive expansion engine",
  "in the modern food and beverage industry."
]
```

测试期望的 S9522 第二页是：

```text
into the most aggressive expansion engine in the modern food and beverage industry.
```

因此 A3 与工单写明的“HEAD 基线 PASS”冲突。按照工单的判定性前提规则，本单停止，不进入 S9513 收窄，也不跑当前树四条单测或 `--only article-display-readability-contract` 收尾回归。

## 只读差异盘点

- 本轮没有编辑任何 `.py` 文件，没有改变 S9513 判据。
- 当前工作树在本轮开始前已经包含多轮未提交修改；`podcast_learning_video.py` 的相关函数位于 `app/core/utils/podcast_learning_video.py:5136`，其中包含谓语完整性 issue-code 集合和 `protected_syntax_cut` 的额外边界判断。
- 基线与当前工作树的文件级差异还包含词卡、分页候选、降级出口和其他历史改动，不能在 A3 不成立时把它们归因于本单。
- 基线副本未修改，冻结 stable-runs 未修改。

## 结论

本单驳回，原因是判定性前提 A3 不成立：当前可复现证据显示 HEAD 基线的 S9522 已经 FAIL。需要先统一基线运行环境或修正 §46.30 的前提证据，再重新授权收窄 S9513。

本轮未执行 `git add`、`git commit`、`git push`、`git stash`、`git restore` 或 `git checkout`。
