# G1 / G7 撤除止血报告

时间：2026-08-26 10:33:34

## 数字

| 项目 | 结果 |
|---|---:|
| 删除 `relative_clause_has_trailing_predicate` 代码/引用 | 5 处 |
| `--only article-display-readability-contract` | FAIL |
| 失败断言 | `tests/test_article_display_readability_contract.py:3251` |
| 运行耗时 | 319.90 秒 |
| 新增持久化测试 | 0（分页验收失败，按要求未继续） |
| 提交哈希 | 无（未达到提交条件） |

## 结论

G7 删除后分页选择验收未恢复通过，必须停在选择层，不能继续提交 G1 或改其他机制。

## 失败断言与实际页面切分

失败测试 `test_high_pressure_secondary_review_rejects_incomplete_phrase_boundaries` 的 `S9513` 用例在第 3251 行断言 `[page["english"] for page in plan["pages"]] == [text]`。

实际页面为两页：

1. `In 2025, the administration of Donald Trump announced that it would aggressively`
2. `revoke visas for Chinese nationals studying in unspecified critical fields.`

同一测试中的 `S9511`、`S9512` 仍分别保持单页。`S9513` 没有抛出测试预期的 `RenderStructuralOverflowError`，因此直接落入第 3251 行断言失败。

## 发现但没动

- 测试期间仍出现 `AppData/logs/app.log` 轮转 `WinError 32` 噪音；本轮不改日志配置。
- 目标文件工作树已有大量与本轮无关的换行/历史改动；不清理、不回退。

## 提交与差异

未提交。目标文件当前工作树差异统计为 `525 insertions(+), 56 deletions(-)`，其中包含本轮之前已有改动，无法在失败状态下安全拆分 G1 提交。
