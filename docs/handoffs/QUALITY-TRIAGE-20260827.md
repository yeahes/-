# 质量任务取舍记录

时间：2026-08-27 13:27:28 +08:00

## 结论

当前主线只保留一项需要立即处理的工作，另有两项低优先级验证：

1. 处理 `中国企业正把供应链铺满全球` 的 `S0136.P01/P02` 缺页和
   `S0260` 否定词错页：只重试失败父句/页面，成功部分保持不动；重试失败
   也不能阻止后续父级翻译质量审计。详细边界见
   `docs/handoffs/LOCAL-RETRY-AND-AUDIT-PLAN-20260827.md`。
2. 需要时重启 GUI 实测一次冻结检查点续跑，确认不会重新跑 ASR、父级翻译和
   WhisperX。
3. 需要时用一集全新、未人工校对的音频测量人工修改率、分页失败率、中文
   问题率、审计召回和耗时。

第 2 项的合成线程说明文字已放弃，不再提交。第 3 项的界面逻辑已由提交
`9b4f7d6` 完成。第 4 项只保留为第 1 项的假响应回归测试计划；官方 DeepSeek
审计代码本身已由提交 `5522275` 完成，真实审计按需执行。

`S9522` 已做最小对照：工作树和 `E:\\vc-head-baseline` 的页面切分完全一致，
均为第一页到 `expansion engine`、第二页从 `in the modern...` 开始。它是
测试期望与当前基线不一致，不是本轮代码造成的回归；不据此修改页面选择逻辑，
也不再作为当前主线任务。

## 停止开发或暂缓

| 方向 | 决定 | 理由 |
| --- | --- | --- |
| `S9522` 分页选择评价 | 暂不评价 | 用户决定不把该测试作为当前决策依据 |
| G7 及更多分页启发式 | 暂缓 | 没有证明整体收益 |
| 完整中英字符/词边界映射 | 暂缓 | 当前产物没有对齐数据，实现复杂，容易制造假切点 |
| 五类语义锚点接入生产阻断 | 暂缓 | 4 集 278 页未发现真实数字错页，误报风险高 |
| 全量翻译重写、增加更多服务商 | 停止 | 成本高，不能解决当前分页阻断 |
| 编辑器大改、历史记录重构、无关视觉优化 | 暂缓 | 不直接提高当前成片率 |

## 影响

停止/暂缓只表示不再把这些方向当作当前开发任务；历史文档、审计结果、
缓存和代码暂不删除，避免破坏可追溯性。当前工作树仍有未提交实验和生成证据，
后续只按机制分组处理。
