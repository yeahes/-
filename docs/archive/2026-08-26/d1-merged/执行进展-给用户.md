# 执行进展

更新时间：2026-08-24 14:39:00

## 2026-08-25：白宫删除字幕后保存失败

做了什么：用桌面终稿包离线重放保存校验，定位到删除区间后的分页页时间仍是原始音频时间，修正为从压缩后的展示词时间重新计算；同时修复合法输出时间 `0` 被当成空值的问题。
预登记门禁值：不改英文、字幕 ID、父字幕词范围、权威词账本；删除区间后的所有分页页必须满足 `end_ms > start_ms`，保存合同必须 PASS。
实测值：白宫终稿包 186 个渲染计划、0 个非法页；`_write_manual_render_contract` 返回 `render_blocked=false / display_status=PASS`。新增删除首段字幕回归夹具通过，分页合同 76/76 通过。
通过情况：本次保存链路通过；尚未替用户写回桌面终稿包，待项目回归和用户重启后的 GUI 保存/合成确认。完整可读性回归另有既存 `S9522` 断言失败（`into` 与 `in`），与本修复无关。

## 测试音频：自动结果审计与发布阻塞修复

做了什么：逐页核对了桌面 `测试音频.MP3` 的首个自动结果；120 条父字幕生成 156 个实际页，重点压力样本 44 条父字幕。没有改写旧产物。
预登记门禁值：核心内容合同必须通过；质量审计可以独立为 PASS 或 PARTIAL，PARTIAL 必须明确列出未覆盖字幕，不能伪装成 PASS。
实测值：时间轴 PASS、空中文页 0、空英文行数组 0、覆盖缺口 0；质量审计 80/120，剩余 S0041–S0080 所在批次因请求超时；实际页序检测发现 S0010、S0031，模型审计发现 S0045。
通过情况：分页/时间轴通过；原流程因 PARTIAL 被错误阻塞，现已改为带警告发布。焦点测试 207/207 通过；代码修改后尚未重跑新音频。

## 本轮通用改动

做了什么：质量审计 PARTIAL/UNAVAILABLE 不再把已通过分页和时间轴的结果判成“优化失败”；manifest、摘要和阶段日志会记录审计覆盖数、缺失 ID、批次错误。新增保守的逐页中文顺序倒置 REVIEW 标记。
预登记门禁值：不改变英文、固定字幕 ID、词账本、时间轴或中文文本；真实硬错误仍必须阻塞。
实测值：当前自动产物只读重放得到 2 个明确顺序倒置候选（S0010、S0031），S0045 由现有模型审计标出；相关 207 个测试全部通过。
通过情况：通用检测和发布合同通过；仍需下一次新音频运行确认 UI 能加载“可用但审计未完整”的稳定结果。

## P1'：中文分页切分离线对照

做了什么：按 §29.3 用三份人工终稿的 100 个多页父字幕，复现当前切分并测试“窗口放宽、标点优先”的确定性候选；只读，不改生产默认值，不重跑人工终稿。
预登记门禁值：合计复现率 ≥70%；就业、梦核、烂到爆红分别 ≥60%；用户改过中文子集分别 ≥60%。
实测值：当前基线 39/100（就业 8/25、梦核 14/46、烂到爆红 17/29）；候选最佳 62/100（14/25、25/46、23/29），用户改过子集 38/69。
没过：就业 56.0%、梦核 54.3%、合计 62.0%，均未达到门禁；原因是同一窗口存在多个标点/词边界，单纯提高标点优先级仍会选错，不能进入 P2'。

补充验证：全局动态规划在 144 组参数中最高 63/100（就业 14/25、梦核 25/46、烂到爆红 24/29；用户改过子集 39/69），仍未过门禁。

## §30 复核

做了什么：阅读 §30，并把它与 P1' 实测和现有终稿历史对照；“页数大多不变、宽度优先级下降、术语表收益低”作为方向性判断保留，但不能覆盖 P1' 未通过的实测。
预登记门禁值：P1' 仍为总复现 ≥70%、三集分别 ≥60%、用户改过子集 ≥60%；P2' 必须等 P1' 通过后再做。
实测值：当前已复现基线 39/100；简单标点优先 62/100；全局评分最高 63/100；桌面 `测试音频.MP3` 存在，大小 7,694,260 bytes，尚未进入 P4。
没过：§30 不能证明当前函数只需调窗口就能过门禁；P1' 连续两种通用候选均未通过，暂不改默认切分、不跑 P2'、不跑新音频生产流程。

## P0：目标函数脚本

做了什么：新增独立只读脚本，按三份登记的人工终稿计算 E、R 的可用基线和字符信号召回，并支持传入任意标记父 ID 集合。
预登记门禁值：就业 237/28/15/61/101/110/24；梦核 201/60/6/149/201/90/43；烂到爆红 173/30/7/75/115/79/24。
实测值：三集全部逐位复现；合并 P=611、GT=118、E=19.3%，字符≥58 信号标出率 45.7%、召回率 77.1%。
通过 / 未通过：通过；P1 可以开始，仍不能把 E、R、Rec 当成字幕语言质量分数。

## P1：中文修改归因表

做了什么：把就业、梦核、烂到爆红三份终稿 history 的 285 次中文修改逐条还原为改前/改后页面，并标注缺陷类型、A/B/C 可检测性和检测器依据。
预登记门禁值：285/285 条覆盖率 100%，必须同时有三个标签，并明确 A 档占比。
实测值：覆盖 285/285（100%）；A=91（31.9%）、B=119（41.8%）、C=75（26.3%）；主要类型为中文不通顺 119、语义漏译 85、纯风格偏好 75。
通过 / 未通过：通过；P2 只能先做 A 档离线标记，理论覆盖上限约 31.9%，不能据此宣称达到 95%。

## 当前路线

按 §29.5 执行：P0 已通过；P1' 未通过，P2' 暂不推进。若继续，必须引入英文页语义/句法证据的离线实验并重新登记门禁；不得把未通过候选接入生产。

## 样本边界

就业、梦核、烂到爆红三份终稿只用于 P0/P1/P2/P3 离线对照，不重跑、不写回旧产物；四个已人工调整案例不是 P4 新素材。

## P4 新素材状态

P4 尚未开始；需要一份没有人工终稿包、没有 `manual-draft-safety-backup`、且未人工校对的新音频。没有新素材时保持挂起。

## 需要你决定的事

- 是否允许做引入英文页语义/句法证据的第三版离线实验（仍不改默认值）；当前两版只用中文和页词数，最高 63/100。
- `测试音频.mp3` 已确认存在于桌面，但 P1' 未通过前不进入 P4；它不会被重跑或写入旧产物。

## C1：短单页连锁语义与语气词离线测量

做了什么：新增只读脚本 `scripts/audit_short_chain_and_backchannel.py`，用 `测试音频` 的 immutable stable run 和人工终稿 history 测量候选信号；没有写回 ledger、缓存、字幕或人工终稿。
预登记门禁值：C1 是测量，不设通过阈值；任何补信号必须先证明召回、读取负担和误标可接受，翻译提示词只做证据对照，不直接上线。
实测值：用户修改父字幕 24 条，现有 marks 命中 14 条、漏标 10 条；短链信号标出 26/120（读取率 21.7%），命中 4/24（召回 16.7%），误标 22 条；语气词相关父 13 条，人工补回证据 3 条、反向删除 1 条。
没过：短链信号不可接生产，覆盖不了 `S0062/S0063`、`S0093/S0094`、`S0117/S0118` 等主要漏标组；语气词结果只能作为后续提示词 A/B 的夹具，不能据此宣称翻译质量已改善。

## C1-边界语义：spaCy 跨父依存离线实验

做了什么：新增只读脚本 `scripts/audit_cross_parent_semantics.py`，在同一 immutable run 上比较右侧词依存到左父字幕的边界信号；没有写回字幕、缓存、审校清单或人工终稿。
预登记门禁值：只允许进入生产的候选必须同时提高漏标召回、控制新增阅读量，并在现有清单合并后不超过当前人工阅读预算；本轮先测，不预设可通过数字。
实测值：宽松候选 11 个边界、22 个父字幕，命中 9/24；扩大邻接组后合并召回 24/24，但阅读量 62/120（51.7%）。保守候选（逗号后只接受直接宾语、非逗号接受从句/补语）6 个边界、12 个父字幕，命中 9/24；与现有清单合并召回 22/24（91.7%），阅读量 40/120（33.3%）。
通过情况：未通过生产接线门禁；它能补到 `S0062/S0063`、`S0093`、`S0104/S0105`、`S0106/S0107`、`S0117/S0118`，但新增阅读量仍高于当前 24.2% 基线，且 `S0094`、`S0103` 没有英文跨父依存证据，不能靠切分信号解决。下一步保留为离线夹具，转查中文页映射/翻译责任层。

## S0：分页候选清零降级（门禁预登记）

做了什么：准备把“只有不完整 REVIEW 边界但页面仍可排版”的候选保留为临时人工复核计划；不改英文切分、字幕 ID、词账本或时间轴，正式发布仍保持 BLOCKER。
预登记门禁值：全库 62 集该原因整集失败 0；页首标点 0、胶合 token 0；棘轮全绿（newly_None=0、空页=0、监督复现至少 62/100、标点切点率至少 55%、无新增回归失败、render_blocked 不变）；日本夹具 S0001=3 页、S0242=2 页且保留 `display_page_blueprint_invalid` BLOCKER；本项单独提交并附反向补丁。
实测值：尚未运行；先补单元夹具和日本 checkpoint 离线重放，结果按上述固定口径记录，不调整门禁。
通过情况：待测。报忧：临时候选只允许进入编辑器和逐页中文分配，不能直接合成；需要用户决定的事：若五条门禁任一失败，回退本提交，不放宽英文切分规则。

## S0：短句误触兜底修正与日本 checkpoint 重放

做了什么：修正候选分支，只有没有正常候选时才生成 REVIEW 兜底；同时保留附着语气词的最短中文表达，独立语气词空译文也会恢复。
预登记门禁值：普通短句不得被改成 REVIEW 多页；日本 S0001=3 页、S0242=2 页，均保持 REVIEW/BLOCKER；翻译不得改英文、ID、时间轴或事实。
实测值：S0/可读性聚焦测试 3/3，稳定页合同 1/1，翻译回归 3/3；日本 checkpoint S0001=3 页、S0242=2 页，两条仍为 REVIEW 兜底，正式路径仍 BLOCKER。
过没过：聚焦门禁通过；全库 62 集棘轮和完整回归尚未跑，不能把本项宣称为全库通过。

## 翻译提示词与人工终稿恢复门禁

做了什么：翻译提示词加入事实/逻辑保留、连续阅读和简洁约束；翻译缓存版本升级到 v8/v5/v10；修复手动分页旧中文恢复时把未确认草稿误当终稿的问题。
预登记门禁值：不改英文、字幕 ID、词账本、时间轴；未确认的手动分页必须继续阻塞；同身份且已确认的页面可以恢复，不重复调用 API。
实测值：翻译规则测试 555/555；人工终稿编辑器测试通过；完整回归 32 项中 30 项通过，剩余 2 项是既有显示分页期望和旧状态夹具不一致。
过没过：翻译和恢复聚焦门禁通过；完整回归未全绿，且当前可见的 `测试音频` 仍是旧提示词 v7/v4 产物，不能据此宣称新提示词已提升观看质量。

## 测试音频长字幕只读审计

做了什么：按长字幕审计技能逐条检查 immutable run 的 43 个高压力父字幕、79 个实际分页及相邻上下文；没有修改任何产物。
预登记门禁值：只报告有明确证据的问题；分页中文必须按实际 Sxxxx.Pxx 顺序判断，不能把黄色标记直接当错误。
实测值：身份和内容绑定 PASS；120 个父字幕、156 页、32 个多页父字幕，68/68 多页中文非空；确认页中文/分页问题集中在 S0004、S0010、S0021、S0023、S0045、S0083、S0096、S0115，其中 S0115 还包含英文分页边界问题。
过没过：旧产物没有阻塞，但它使用 v7/v4 提示词，只能作为基线；需要一份当前 v8/v5/v10 的新自动结果才能验证翻译改动是否真正减少人工复核。

## ASR 有声缺口阻塞门禁

做了什么：把 Faster-Whisper 的内部缺口修复证据传到 ASR 结果；局部重转无法双侧锚定、返回空结果或只返回残片时，稳定词级字幕流程在英文冻结前直接停止，不再静默生成缺词字幕。
预登记门禁值：已确认有声且未修复的 1.8-15 秒缺口不得进入稳定切分、翻译和分页；成功双锚点修复继续保持原行为。
实测值：ASR trust 聚焦测试 43/43；完整回归 31/32 模块通过，唯一失败是既有 `test_three_line_fallback_promotes_complete_two_page_alternative` 版式期望不一致，与本次 ASR 改动无调用关系；日志另有既存 `app.log` 轮转锁定警告。
过没过：ASR 门禁聚焦通过；还需要重启程序并重新跑“新白宫”，确认真实音频缺口能被补回，或在无法可靠补回时明确停在 ASR 阶段。
