## 2026-08-23 - Fixed-Parent Production Selection And Recovery List

- Promoted the fixed-parent material-readability experiment into production as
  planner v32. The owner remains the renderer page selector: it cannot change
  frozen English, IDs, word ownership, timing, or parent Chinese.
- Real newest-White-House replay rebuilt all 217 render plans and changed only
  `S0072/S0097/S0201/S0205`; the three known structural failures remain
  editable blockers rather than being hidden by a fallback.
- Recovery discovery now returns one current entry per audio title, prioritizes
  unsaved drafts, and scans the exact source-adjacent manual package directory.
  Real discovery collapses the White House history to one visible entry.
- Applied the existing native dark/light review-dialog palette to recent-result
  recovery and exposed the exact future manual-package directory in each row.
- The affected page-selector, manual-editor, and publication tests pass
  (`223 passed`). Syntax and diff checks pass. The user owns the complete
  regression run for this stage.

